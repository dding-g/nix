{ ... }:

{
  # ═══════════════════════════════════════════════════════════
  # Starship (프롬프트)
  # Nix가 starship 설치 + zsh 연동 + 설정 모두 관리
  # ═══════════════════════════════════════════════════════════
  programs.starship = {
    enable = true;
    enableZshIntegration = true;  # .zshrc에 eval "$(starship init zsh)" 자동 추가

    settings = {
      # ─────────────────────────────────────────
      # 기본 설정
      # ─────────────────────────────────────────
      add_newline = true;
      scan_timeout = 10;

      # ─────────────────────────────────────────
      # 프롬프트 포맷 (원하는 순서로 정렬)
      # ─────────────────────────────────────────
      format = ''
        $directory$git_branch$git_status$fill$cmd_duration$line_break$character
      '';

      right_format = ''
        $nodejs$python$aws$nix_shell
      '';

      # ─────────────────────────────────────────
      # 문자 (프롬프트 심볼)
      # ─────────────────────────────────────────
      character = {
        success_symbol = "[❯](bold green)";
        error_symbol = "[❯](bold red)";
        vimcmd_symbol = "[❮](bold green)";
      };

      # ─────────────────────────────────────────
      # 디렉토리
      # ─────────────────────────────────────────
      directory = {
        truncation_length = 4;
        truncate_to_repo = true;
        style = "bold cyan";
        read_only = " 🔒";
        home_symbol = "~";
      };

      # ─────────────────────────────────────────
      # Git
      # ─────────────────────────────────────────
      git_branch = {
        symbol = " ";
        style = "bold purple";
        format = "[$symbol$branch]($style) ";
      };

      git_status = {
        style = "bold red";
        format = "([$all_status$ahead_behind]($style))";
        conflicted = "✖";
        ahead = "⇡$count";
        behind = "⇣$count";
        diverged = "⇕";
        untracked = "?$count";
        stashed = "📦";
        modified = "!$count";
        staged = "+$count";
        deleted = "✘$count";
      };

      # ─────────────────────────────────────────
      # 명령 실행 시간
      # ─────────────────────────────────────────
      cmd_duration = {
        min_time = 2000;  # 2초 이상만 표시
        format = "[$duration]($style) ";
        style = "bold yellow";
      };

      # ─────────────────────────────────────────
      # 언어/런타임 (오른쪽 표시)
      # ─────────────────────────────────────────
      nodejs = {
        symbol = " ";
        style = "bold green";
        format = "[$symbol($version)]($style) ";
      };

      python = {
        symbol = " ";
        style = "bold blue";
        format = "[$symbol($version)]($style) ";
      };

      aws = {
        symbol = " ";
        style = "bold yellow";
        format = "[$symbol($profile)]($style) ";
      };

      nix_shell = {
        symbol = " ";
        style = "bold blue";
        format = "[$symbol$state]($style) ";
      };

      # ─────────────────────────────────────────
      # fill (왼쪽/오른쪽 분리)
      # ─────────────────────────────────────────
      fill = {
        symbol = " ";
      };
    };
  };
}
